# Frontend environment variables
# Production on Vercel uses same-origin API routes (/api), so this can stay empty.
VITE_API_BASE_URL=

# Local development only. Used by Vite dev proxy when backend runs separately.
# Example: http://localhost:5000
VITE_DEV_API_BASE_URL=http://localhost:5000
